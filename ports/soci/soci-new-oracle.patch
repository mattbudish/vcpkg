diff --git a/cmake/modules/FindOracle.cmake b/cmake/modules/FindOracle.cmake
index 97997c61..3a71f877 100644
--- a/cmake/modules/FindOracle.cmake
+++ b/cmake/modules/FindOracle.cmake
@@ -23,8 +23,8 @@
 ###############################################################################
 
 # First check for CMAKE  variable
-if(NOT ORACLE_HOME)
-  # If ORACLE_HOME is not defined check for env var and if exists set from env var
+if(NOT EXISTS ORACLE_HOME)
+    # If ORACLE_HOME is not defined check for env var and if exists set from env var
   if(EXISTS $ENV{ORACLE_HOME})
     set(ORACLE_HOME $ENV{ORACLE_HOME})
   endif()
@@ -42,9 +42,14 @@ find_path(ORACLE_INCLUDE_DIR
   # instant client from rpm
   /usr/include/oracle/*/client${LIB_SUFFIX})
 
-set(ORACLE_OCI_NAMES clntsh libclntsh oci) # Dirty trick might help on OSX, see issues/89
-set(ORACLE_OCCI_NAMES libocci occi oraocci10 oraocci11 oraocci12)
-set(ORACLE_NNZ_NAMES nnz10 libnnz10 nnz11 libnnz11 nnz12 libnnz12 ociw32)
+  set(ORACLE_VERSIONS 19 18 12 11 10)
+  set(ORACLE_OCI_NAMES clntsh libclntsh oci)
+  set(ORACLE_OCCI_NAMES libocci occi)
+  set(ORACLE_NNZ_NAMES ociw32)
+  foreach(loop_var IN LISTS ORACLE_VERSIONS)
+    set(ORACLE_OCCI_NAMES ${ORACLE_OCCI_NAMES} oraocci${loop_var})
+    set(ORACLE_NNZ_NAMES ${ORACLE_NNZ_NAMES} nnz${loop_var} libnnz${loop_var} orannzsbb${loop_var})
+  endforeach(loop_var)
 
 set(ORACLE_LIB_DIR
   ${ORACLE_HOME}
@@ -78,4 +83,9 @@ set(ORACLE_LIBRARIES ${ORACLE_LIBRARY})
 include(FindPackageHandleStandardArgs)
 find_package_handle_standard_args(ORACLE DEFAULT_MSG ORACLE_LIBRARY ORACLE_INCLUDE_DIR)
 
+if(NOT ORACLE_FOUND)
+  message(STATUS "None of the supported Oracle versions (${ORACLE_VERSIONS}) could be found, "
+                 "consider updating ORACLE_VERSIONS if the version you use is not among them.")
+endif()
+
 mark_as_advanced(ORACLE_INCLUDE_DIR ORACLE_LIBRARY)
